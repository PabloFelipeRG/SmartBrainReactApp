import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent:'flex-end'}}>
            <p className='f3 fw6 db white link dim pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;